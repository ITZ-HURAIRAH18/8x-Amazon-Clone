---
session_id: ses_f28154140ffeZhzubAyiKY4qvi
date: 2026-09-25
author: ITZ-HURAIRAH18
model: opencode/space-bunny-free
tool: opencode
project: 8x Assignment
total_exchanges: 5
first_prompt_time: 2026-09-25T09:34:42.006Z
last_prompt_time: 2026-09-25T10:17:36.451Z
---

# Session Log - 2026-09-25

Session: `ses_f28154140ffeZhzubAyiKY4qvi` | Project: `8x Assignment` | Author: `ITZ-HURAIRAH18`

---
[LOG_ENTRY type=PROMPT num=1 session=ses_f28154140ffeZhzubAyiKY4qvi]
timestamp: 2026-09-25T09:34:42.006Z
model: opencode/space-bunny-free

when i run backend it show 50000 port is already in use or also when i used 5001 it throw error fix it

[LOG_ENTRY type=RESPONSE num=1 session=ses_f28154140ffeZhzubAyiKY4qvi]
timestamp: 2026-09-25T09:42:22.754Z
model: opencode/space-bunny-free

Fixed the backend port issue.

### What was wrong

- An older Node backend process was already listening on port `5000`.
- A failed `node --watch` backend process was also still running.
- Port `5001` is valid and was tested successfully; it was not the root problem.

### Changes made

- Cleaned up the stale backend processes.
- Added proper `EADDRINUSE` handling in `server/src/server.js`, so future conflicts show a clear message instead of a raw stack trace.
- Documented the correct startup commands in `README.md`.

### Current status

A clean backend is running on:

```text
http://localhost:5000
```

Health check verified successfully:

```json
{
  "data": {
    "status": "ok",
    "database": "connected"
  }
}
```

### How to run it

Run the full application once from the project root:

```bash
npm run dev
```

Or run only the backend:

```bash
npm run dev --workspace server
```

Do **not** run both commands at the same time. If you want to use port `5001` in PowerShell:

```powershell
$env:PORT=5001
npm run dev --workspace server
```

Also update `client/.env` to match:

```env
VITE_API_URL=http://localhost:5001/api
```

[LOG_ENTRY type=PROMPT num=2 session=ses_f28154140ffeZhzubAyiKY4qvi]
timestamp: 2026-09-25T09:44:43.383Z
model: opencode/space-bunny-free

see error.md file and fix it for vercel

[LOG_ENTRY type=RESPONSE num=2 session=ses_f28154140ffeZhzubAyiKY4qvi]
timestamp: 2026-09-25T09:44:49.700Z
model: opencode/space-bunny-free

I’ll inspect `error.md`, trace the Vercel deployment/configuration it refers to, and fix the underlying deployment issue rather than only masking the error.

[LOG_ENTRY type=PROMPT num=3 session=ses_f28154140ffeZhzubAyiKY4qvi]
timestamp: 2026-09-25T09:59:43.618Z
model: opencode/space-bunny-free

yes continue above

[LOG_ENTRY type=PROMPT num=4 session=ses_f28154140ffeZhzubAyiKY4qvi]
timestamp: 2026-09-25T10:09:57.937Z
model: opencode/space-bunny-free

hnji kiya bana

[LOG_ENTRY type=PROMPT num=5 session=ses_f28154140ffeZhzubAyiKY4qvi]
timestamp: 2026-09-25T10:17:36.451Z
model: opencode/space-bunny-free

ha kab tak ho jay ga

[LOG_ENTRY type=RESPONSE num=3 session=ses_f28154140ffeZhzubAyiKY4qvi]
timestamp: 2026-09-25T10:22:22.242Z
model: opencode/space-bunny-free

Ho gaya hai — **code side Vercel fix ready hai**.Live 404 abhi old deployment ki wajah se hai; Vercel par naya deployment push/redeploy karna baaki hai.

### Vercel backend settings

Backend Vercel project mein:

- **Root Directory:** `server` (recommended)
- **Framework Preset:** Other
- **Install Command:** `npm install`
- **Build Command:** blank
- **Output Directory:** blank

Backend project ko `client` par set na karein.

### Backend environment variables

Vercel mein ye add karein:

```env
MONGODB_URI=your_mongodb_connection_string
JWT_SECRET=your_long_random_secret
CLIENT_URL=https://amazon-clone-client-five.vercel.app
NODE_ENV=production
```

Frontend mein ye rehna chahiye:

```env
VITE_API_URL=https://8x-amazon-clone-server.vercel.app/api
```

### Code push karein

```bash
git add README.md server/api/index.js server/src/app.js vercel.json server/vercel.json api/index.js
git commit -m "fix vercel backend deployment"
git push origin main
```

Phir Vercel mein **Redeploy** karein. Deployment Protection public API traffic ke liye disabled honi chahiye.

### Verify

Ye URL open karein:

```text
https://8x-amazon-clone-server.vercel.app/api/health
```

Expected response:

```json
{
  "data": {
    "status": "ok",
    "database": "connected"
  }
}
```

Local Vercel test mein `/api/health` **200**, login request **401 with valid JSON**, aur CORS header successfully verify ho chuka hai. 404 aur CORS error old deployment ki wajah se tha; deployment update ke baad login work karega.

[LOG_ENTRY type=RESPONSE num=4 session=ses_f28154140ffeZhzubAyiKY4qvi]
timestamp: 2026-09-25T10:22:22.242Z
model: opencode/space-bunny-free

Ho gaya hai — **code side Vercel fix ready hai**.Live 404 abhi old deployment ki wajah se hai; Vercel par naya deployment push/redeploy karna baaki hai.

### Vercel backend settings

Backend Vercel project mein:

- **Root Directory:** `server` (recommended)
- **Framework Preset:** Other
- **Install Command:** `npm install`
- **Build Command:** blank
- **Output Directory:** blank

Backend project ko `client` par set na karein.

### Backend environment variables

Vercel mein ye add karein:

```env
MONGODB_URI=your_mongodb_connection_string
JWT_SECRET=your_long_random_secret
CLIENT_URL=https://amazon-clone-client-five.vercel.app
NODE_ENV=production
```

Frontend mein ye rehna chahiye:

```env
VITE_API_URL=https://8x-amazon-clone-server.vercel.app/api
```

### Code push karein

```bash
git add README.md server/api/index.js server/src/app.js vercel.json server/vercel.json api/index.js
git commit -m "fix vercel backend deployment"
git push origin main
```

Phir Vercel mein **Redeploy** karein. Deployment Protection public API traffic ke liye disabled honi chahiye.

### Verify

Ye URL open karein:

```text
https://8x-amazon-clone-server.vercel.app/api/health
```

Expected response:

```json
{
  "data": {
    "status": "ok",
    "database": "connected"
  }
}
```

Local Vercel test mein `/api/health` **200**, login request **401 with valid JSON**, aur CORS header successfully verify ho chuka hai. 404 aur CORS error old deployment ki wajah se tha; deployment update ke baad login work karega.

[LOG_ENTRY type=RESPONSE num=5 session=ses_f28154140ffeZhzubAyiKY4qvi]
timestamp: 2026-09-25T10:22:22.242Z
model: opencode/space-bunny-free

Ho gaya hai — **code side Vercel fix ready hai**.Live 404 abhi old deployment ki wajah se hai; Vercel par naya deployment push/redeploy karna baaki hai.

### Vercel backend settings

Backend Vercel project mein:

- **Root Directory:** `server` (recommended)
- **Framework Preset:** Other
- **Install Command:** `npm install`
- **Build Command:** blank
- **Output Directory:** blank

Backend project ko `client` par set na karein.

### Backend environment variables

Vercel mein ye add karein:

```env
MONGODB_URI=your_mongodb_connection_string
JWT_SECRET=your_long_random_secret
CLIENT_URL=https://amazon-clone-client-five.vercel.app
NODE_ENV=production
```

Frontend mein ye rehna chahiye:

```env
VITE_API_URL=https://8x-amazon-clone-server.vercel.app/api
```

### Code push karein

```bash
git add README.md server/api/index.js server/src/app.js vercel.json server/vercel.json api/index.js
git commit -m "fix vercel backend deployment"
git push origin main
```

Phir Vercel mein **Redeploy** karein. Deployment Protection public API traffic ke liye disabled honi chahiye.

### Verify

Ye URL open karein:

```text
https://8x-amazon-clone-server.vercel.app/api/health
```

Expected response:

```json
{
  "data": {
    "status": "ok",
    "database": "connected"
  }
}
```

Local Vercel test mein `/api/health` **200**, login request **401 with valid JSON**, aur CORS header successfully verify ho chuka hai. 404 aur CORS error old deployment ki wajah se tha; deployment update ke baad login work karega.

